datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Book {
  gutenId     Int      @id
  title       String
  author      String
  publishDate DateTime
  language    String
  createdAt   DateTime @default(now())
}
